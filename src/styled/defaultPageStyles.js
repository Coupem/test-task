import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  defaultText: {
    fontSize: 40,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: 200,
  },
}));
